#include  <cstdio>
#include  <cstring>

using namespace std;

char a[55],b[55];

int main()
{
    int n;
    while(scanf("%d", &n)==1)
    {
        memset(a,0,sizeof(a));
        memset(b,0,sizeof(b));
        scanf("%s%s", a,b);
    }
}
