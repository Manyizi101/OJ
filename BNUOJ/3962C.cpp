#include  <cstdio>
#include  <cstring>

using namespace std;

char a[100010];

int main()
{
    int t,i;
    scanf("%d", &t);
    while(t--)
    {
        memset(a, 0, sizeof(a));
        scanf("%s", a);
        w
    }
}
