#include   <stdio.h>


int main()
{
    char type[10];
    int n, i, data[10];
    scanf("%s%d", &type[0], &n);
    for (i = 0; i < n; i++)     scanf("%d", &data[i]);
    if (type == 'Faster')
    return 0;
}
