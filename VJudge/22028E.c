#include  <stdio.h>
#include  <iostream>
#include  <algorithm>>
#define   MAX  100000


int main()
{
    int N, C;
    while(scanf("%d%d", &N,&C)&&N!=0)
    {
        char number[MAX][6],name[MAX][8];
        int grade[MAX];
        for(i=0;i<N;i++)    scanf("%s%s%d", number[i], name[i], &grade[i]);

    }
}
